00: print with no args, values for string and length are hard-coded
01: print with args passed in via registers, values still hard-coded
02: print with args passed in via stack frame, string is static, length is dynamic
03: print with args passed in via the stack